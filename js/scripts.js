function add(number1, number2) {
  return number1 + number2;
}

const number1 = parstInt(prompt("Enter a number:"));
const number2 = parseInt(prompt("Enter another number:"));

alert(add(number1, number2));
